public class Expressions {
    public static void main(String[] args) {

    // a mile is equal to 1.609344
        double kilometres =(100*1.609344); // the expression is "kilometres =(100*1.609344)"
        int highScore = 49; // the expression is "highScore = 49"
        ++highScore;
        if (highScore==50) {
            System.out.println("This is an expression");// the expression is ""This is an expression""
            System.out.println(kilometres);

            int score =100; // the expression is "score =100"
            if (score>99){ // the expression is "score>99"
                System.out.println("You got the high score!");// the expression is ""You got the high score!""
                score = 0;// the expression is "score = 0"

                System.out.println(score);

            }
        }

    }
}
